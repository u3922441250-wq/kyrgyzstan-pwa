{
  "name": "kyrgyzstan-2025",
  "version": "1.0.0",
  "description": "Guida di viaggio offline per il Kirghizistan 2025",
  "private": true,
  "scripts": {
    "build": "node build-www.js",
    "cap:add-android": "cap add android",
    "cap:copy": "npm run build && cap copy android",
    "cap:sync": "npm run build && cap sync android",
    "cap:open": "cap open android",
    "cap:build": "npm run build && cap sync android && cap open android",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "fake-indexeddb": "^6.2.5",
    "fast-check": "^4.8.0",
    "http-server": "^14.1.1",
    "jsdom": "^29.1.1",
    "sharp": "^0.34.5",
    "vitest": "^4.1.7"
  }
}
